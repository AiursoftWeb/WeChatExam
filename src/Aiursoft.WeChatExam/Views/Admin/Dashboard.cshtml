@{
    ViewData["Title"] = "管理控制台";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .dashboard-container {
        max-width: 1200px;
        margin: 0 auto;
    }

    .welcome-card {
        background: rgba(255, 255, 255, 0.95);
        padding: 2rem;
        border-radius: 16px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        margin-bottom: 2rem;
    }

    .welcome-card h1 {
        color: #2d3748;
        margin-bottom: 0.5rem;
        font-size: 2rem;
    }

    .welcome-card p {
        color: #718096;
        font-size: 1.1rem;
    }

    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1.5rem;
        margin-bottom: 2rem;
    }

    .stat-card {
        background: rgba(255, 255, 255, 0.95);
        padding: 2rem;
        border-radius: 16px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
    }

    .stat-icon {
        font-size: 2.5rem;
        margin-bottom: 1rem;
    }

    .stat-value {
        font-size: 2.5rem;
        font-weight: bold;
        color: #667eea;
        margin-bottom: 0.5rem;
    }

    .stat-label {
        color: #718096;
        font-size: 1rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .users-table-card {
        background: rgba(255, 255, 255, 0.95);
        padding: 2rem;
        border-radius: 16px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    }

    .users-table-card h2 {
        color: #2d3748;
        margin-bottom: 1.5rem;
        font-size: 1.5rem;
    }

    table {
        width: 100%;
        border-collapse: collapse;
    }

    thead {
        background: #667eea;
        color: white;
    }

    th {
        padding: 1rem;
        text-align: left;
        font-weight: 600;
    }

    td {
        padding: 1rem;
        border-bottom: 1px solid #e2e8f0;
        color: #4a5568;
    }

    tbody tr:hover {
        background: #f7fafc;
    }

    .badge {
        display: inline-block;
        padding: 0.25rem 0.75rem;
        border-radius: 12px;
        font-size: 0.85rem;
        font-weight: 600;
    }

    .badge-admin {
        background: #fed7d7;
        color: #c53030;
    }

    .badge-wechat {
        background: #c6f6d5;
        color: #276749;
    }

    .empty-state {
        text-align: center;
        padding: 3rem;
        color: #a0aec0;
    }

    .empty-state-icon {
        font-size: 4rem;
        margin-bottom: 1rem;
    }
</style>

<div class="dashboard-container">
    <div class="welcome-card">
        <h1>👋 欢迎，管理员!</h1>
        <p>当前时间: @DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss")</p>
    </div>

    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-icon">👥</div>
            <div class="stat-value">@ViewData["TotalUsers"]</div>
            <div class="stat-label">总用户数</div>
        </div>

        <div class="stat-card">
            <div class="stat-icon">📱</div>
            <div class="stat-value">@ViewData["WeChatUsers"]</div>
            <div class="stat-label">微信用户</div>
        </div>

        <div class="stat-card">
            <div class="stat-icon">🔐</div>
            <div class="stat-value">@ViewData["AdminUsers"]</div>
            <div class="stat-label">管理员</div>
        </div>
    </div>

    <div class="users-table-card">
        <h2>📋 用户列表</h2>

        @{
            var users = ViewData["Users"] as List<Aiursoft.WeChatExam.Entities.User>;
        }

        @if (users == null || !users.Any())
        {
            <div class="empty-state">
                <div class="empty-state-icon">📭</div>
                <p>暂无用户数据</p>
            </div>
        }
        else
        {
            <table>
                <thead>
                    <tr>
                        <th>用户名</th>
                        <th>显示名称</th>
                        <th>类型</th>
                        <th>OpenId</th>
                        <th>创建时间</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in users)
                    {
                        <tr>
                            <td>@user.UserName</td>
                            <td>@user.DisplayName</td>
                            <td>
                                @if (!string.IsNullOrEmpty(user.MiniProgramOpenId))
                                {
                                    <span class="badge badge-wechat">微信用户</span>
                                }
                                else
                                {
                                    <span class="badge badge-admin">管理员</span>
                                }
                            </td>
                            <td>@(user.MiniProgramOpenId ?? "-")</td>
                            <td>@user.CreationTime.ToString("yyyy-MM-dd HH:mm")</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>
</div>
